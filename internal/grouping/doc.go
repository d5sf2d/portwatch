// Package grouping provides a Registry that maps logical group names to sets
// of port numbers. Groups allow operators to label related ports (e.g.
// "web", "database", "monitoring") so that alerts and reports can surface
// context alongside raw port numbers.
package grouping
