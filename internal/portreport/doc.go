// Package portreport tracks open/close events per port across scan cycles
// and exposes a Report summarising cumulative counts, first-seen and
// last-seen timestamps, and optional service labels.
package portreport
